"""
LEVEL 3 — Top K Problems

Exercise 7 — Top K largest numbers
-----------------------------------
Given a list of numbers and k, return the k largest numbers in descending order.
Use a heap; do not sort the entire list.

Expected output for EXAMPLE_NUMS and EXAMPLE_K: [14, 9, 8]

Constraints:
- Use a heap
- Do not sort the entire list
"""

EXAMPLE_NUMS = [5, 1, 9, 3, 14, 7, 8]

EXAMPLE_K = 3

EXPECTED_OUTPUT = [14, 9, 8]


def top_k_largest(nums: list[int], k: int) -> list[int]:
    """Return the k largest numbers in descending order, using a heap."""
    pass

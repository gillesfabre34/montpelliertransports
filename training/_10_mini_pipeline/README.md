# 10 — Mini analytical pipeline

Single dataset (users, orders, events, products); multiple exercises: top products, top users, revenue by country, cumulative metrics, deduplication. Combine previous patterns.

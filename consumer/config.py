"""Shared configuration: Kafka (producer + consumer) and related settings."""

# --- Kafka (shared producer / consumer) ---
KAFKA_BOOTSTRAP_SERVERS = "localhost:9092"
KAFKA_TOPIC_VEHICLE_POSITIONS_RAW = "vehicle_positions_raw"

# --- Azure ---